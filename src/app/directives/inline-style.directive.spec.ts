import { InlineStyleDirective } from './inline-style.directive';

describe('InlineStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new InlineStyleDirective();
    expect(directive).toBeTruthy();
  });
});
